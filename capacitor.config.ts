import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hg_desarrolloInterfaces_1tr',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
